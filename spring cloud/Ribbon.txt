Spring Cloud Ribbon 是一个基于HTTP和TCP的客户端负载均衡工具。

负载均衡器组件的功能：
    1）维护服务器的IP、DNS等信息
    2）根据特定的逻辑在服务器列表中循环

Ribbon的负载均衡器三大模块：
    1）Rule：一个逻辑组件，决定从服务器列表中返回哪个服务器实例。
    2）Ping：使用定时器来确保服务器网络可以连接。
    3）ServerList：服务列表，可以通过静态的配置确定负载的服务器，也可以动态指定服务器列表。